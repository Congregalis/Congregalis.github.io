{
  "name": "personal-site",
  "description": "XXX / Claw Archive interactive personal site",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:notes": "node scripts/sync-note-pages.mjs",
    "dev": "npm run sync:notes && vite",
    "build": "npm run sync:notes && vite build",
    "preview": "vite preview",
    "test:e2e": "npm run sync:notes && playwright test"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/instrument-serif": "^5.2.8",
    "@fontsource/manrope": "^5.2.8",
    "gsap": "^3.14.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^8.0.1"
  }
}
